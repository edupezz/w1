import styled from "styled-components";
import { COLORS } from "../../utils/colors";

export const LogoContainer = styled.div`

    display: flex;
    width: 100%;
    margin-top: 44px;
    margin-left: 10px;
    border-bottom: 1px solid ${COLORS.SIDE_PANEL_HIGHLIGHTED};
    flex-grow: 1;
`